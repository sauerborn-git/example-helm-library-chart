apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "examplelibrarychart.fullname" . }}-test-connection"
  labels:
    {{- include "examplelibrarychart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "examplelibrarychart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
